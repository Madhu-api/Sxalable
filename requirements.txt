psycopg2-binary==2.9.11
sshtunnel==0.4.0
paramiko==4.0.0
selenium==4.36.0
pyyaml==6.0.3
pytest==8.4.2
# Locked requests pkg version to 2.32.5 (vulnerable) due to Python 3.9 constraint.
# Upgrade to >=2.33.0 once environment moves to Python 3.11.
requests==2.32.5
webdriver-manager==4.0.2
